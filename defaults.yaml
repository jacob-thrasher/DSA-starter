primary_device: 'cuda'
seed: 69
exp_details:
  data_root: C:\Users\jthra\OneDrive\Documents\data\ADNI
  out_dir: C:\Users\jthra\OneDrive\Documents\data\ADNI # Model + figure path
  exp_id:          # Optional suffix for autogenerated experiment names
  exist_ok: True
  dataset: ADNI_tab
train_params:
  epochs: 50
  checkpoint: 100
  batch_size: 128
  loss_fn: 'DeepHit'
  reduction: 'mean'   # [sum, mean] --> Default: mean
  lambda: 1.0           # Ranking loss weight
model:
  backbone: mlp  # [resnet18, resnet50, vit]
  in_dim: 10            # For nonimaging data 
  mlp_hid_dim: 28     # Mlp only
  pretrained_path: 
optimizer:
  optim: 'Adam'     # [SGD, Adam] --> Default: Adam
  lr: .0001  
  weight_decay: 0   # Usually 0
  momentum: 0       # Usually 0
  lr_scheduler: 
